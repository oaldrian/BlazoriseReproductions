@page "/"

<Heading>How to reproduce</Heading>

Hit Backspace without entering anything else first. In certain conditions the Autocomplete excepts.

<Heading>Case 1</Heading>

SelectedTexts is not bound.

<Autocomplete TItem="string"
              TValue="string"
              Data="@data"
              TextField="@(( item ) => item)"
              ValueField="@(( item ) => item)"
              Placeholder="Search..."
              SelectionMode="AutocompleteSelectionMode.Checkbox"
              @bind-SelectedValues="multipleSelectionData">

</Autocomplete>

<Heading>Case 2</Heading>

SelectedTexts is bound but null.

<Autocomplete TItem="string"
              TValue="string"
              Data="@dataEmpty"
              TextField="@(( item ) => item)"
              ValueField="@(( item ) => item)"
              Placeholder="Search..."
              SelectionMode="AutocompleteSelectionMode.Checkbox"
              @bind-SelectedValues="multipleSelectionData"
              @bind-SelectedTexts="multipleSelectionTextsNull">
</Autocomplete>

<Heading>Case 2 - Working</Heading>

SelectedTexts is bound and not null.

<Autocomplete TItem="string"
              TValue="string"
              Data="@dataEmpty"
              TextField="@(( item ) => item)"
              ValueField="@(( item ) => item)"
              Placeholder="Search..."
              SelectionMode="AutocompleteSelectionMode.Checkbox"
              @bind-SelectedValues="multipleSelectionData"
              @bind-SelectedTexts="multipleSelectionTexts">
</Autocomplete>

@code {
  public List<string> data = new List<string>() { "a", "b", "c", "d" };
  public List<string> dataEmpty = new List<string>();

  List<string> multipleSelectionData;
  List<string> multipleSelectionTexts = new List<string>();
  List<string> multipleSelectionTextsNull;
}